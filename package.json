{
  "name": "MVP Foundry Boilerplate",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"nodemon\" \"browser-sync start --proxy 'localhost:3000' --files 'views/**/*.html, public/**/*.css, public/**/*.js' --no-notify --no-open\"",
    "build:css": "npx tailwindcss -i ./styles.css -o ./public/styles.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/forms": "^0.5.9",
    "autoprefixer": "^10.4.20",
    "chokidar": "^3.6.0",
    "express": "^4.21.0",
    "nunjucks": "^3.2.4",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.11"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.7"
  }
}